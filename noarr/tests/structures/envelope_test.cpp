#include <catch2/catch.hpp>

#include <cstddef>

#include <noarr/pipelines/compute_node.hpp>
#include <noarr/pipelines/link.hpp>
#include <noarr/pipelines/envelope.hpp>

TEST_CASE("It works", "[envelope]") {
	
    // auto env = envelope<std::size_t, float>();
    // link* write_link = env.create_link(link::WRITE | link::EXCLUSIVE);
	// link* read_link = env.create_link(link::READ | link::EXCLUSIVE);

	// ... TODO ...
}
